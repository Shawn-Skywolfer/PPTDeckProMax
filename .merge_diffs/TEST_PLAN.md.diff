diff --git a/TEST_PLAN.md b/TEST_PLAN.md
index 1b1f3f5..e4f0982 100644
--- a/TEST_PLAN.md
+++ b/TEST_PLAN.md
@@ -213,4 +213,4 @@
 3. **数据持久化**：配置后刷新 → 验证恢复
 4. **错误处理**：断开网络 → 验证错误提示
 
- playwright 测试文件已部分存在（`e2e-test*.js`），可在此基础上扩展。
+建议新增 Playwright 冒烟用例（如 `tests/e2e-smoke.spec.js`）作为基础回归入口，再逐步扩展完整 E2E 覆盖。
